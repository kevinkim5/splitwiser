import { defineConfig } from '@chakra-ui/react'

// Foundations

const config = defineConfig({
  globalCss: {
    'html, body': {
      margin: 0,
      padding: '1em',
    },
  },
})

export default config
